# Python
__pycache__

# Files too big to be tracked

# CARLA
docs/CARLA2026/daxpy_validation/daxpy_trace.txt
docs/CARLA2026/MinorFlow_Final_Submission.zip

# Testing: Traces and listings are never committed
*_trace.txt
*_trace.*.txt
*.list

# A tracer JSON in tests/ is what a sample is made from, not content: the
# samples themselves, the .sample.js files and samples.js, are committed.
tests/*.json

# make_MinorFlow_oversized.py output, with the side files of a write it
# did not finish
oversized*.json*

# The CVA6 fork's configurations, linked in by hand
configs/CVA6
